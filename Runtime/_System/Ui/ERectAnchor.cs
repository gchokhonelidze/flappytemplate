namespace FlappyTemplate
{
    // A point on a RectTransform, named by where it sits in the rect.
    public enum ERectAnchor
    {
        TopLeft,
        Top,
        TopRight,
        Left,
        Center,
        Right,
        BottomLeft,
        Bottom,
        BottomRight,
    }
}
